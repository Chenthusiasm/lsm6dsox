# Change the root folder to include source files in src/
#root = ../../

# Only run gcovr on src files found in the source root
filter = ../../

# Exclude the following folders (mainly for the Google test framework)
#exclude = _deps/
#exclude = ../googletest/

html-details = yes
exclude-throw-branches = yes
output = coverage.html
